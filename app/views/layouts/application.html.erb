<!DOCTYPE html>
<html>
	<%= render 'layouts/header' %>
	<body>
		<%= render 'layouts/nav' %>
			<% flash.each do |key, value| %> 
				<div id="error_explanation <%= key %>"><%= value %></div>
			<% end %>
			<%= yield %>
		
		<%= render 'layouts/footer' %>
		<%= debug(params) if Rails.env.development? %>
	</body>
</html>
